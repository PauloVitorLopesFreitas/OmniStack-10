import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//Renderiza tudo que esta dentro do arquivo (App.js) e inse dentro do elemento com id (root) que no caso é uma (div)
ReactDOM.render(<App />, document.getElementById('root'));

